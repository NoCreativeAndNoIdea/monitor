import { initLogger, loadConfigs, report, setPrefix } from './common'

export type { ReportData, RequestData } from './common'

export { initBrowser } from './browser'
export { loadConfigs, initLogger, report, setPrefix }
